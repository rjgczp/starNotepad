# Copy to android/keystore.properties and replace all placeholder values.
# Keep the keystore and this copied file private; both are ignored by Git.
storeFile=keystore/release.jks
storePassword=replace-with-keystore-password
keyAlias=release
keyPassword=replace-with-key-password
